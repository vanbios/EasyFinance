<?xml version="1.0" encoding="utf-8"?>

    <RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/main_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    <include layout="@layout/toolbar" />

    <com.astuetz.PagerSlidingTabStrip
        android:id="@+id/tabs"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        app:pstsShouldExpand="true"
        app:pstsIndicatorColor="@color/custom_deep_purple"
        app:pstsIndicatorHeight="4dp"
        app:pstsUnderlineColor="@color/custom_divider"
        app:pstsUnderlineHeight="1dp"
        app:pstsTextStyle="normal"
        android:textColor="@color/custom_dark_primary"
        app:pstsTextColorSelected="@color/custom_dark_primary"
        android:layout_marginTop="@dimen/toolbar_height"/>


    <android.support.v4.view.ViewPager
        android:id="@+id/pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <android.support.design.widget.CoordinatorLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/coordinateLayoutFloatMain"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="end|bottom">


    <android.support.design.widget.FloatingActionButton
        android:id="@+id/buttonFloat"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|bottom"
        android:layout_margin="@dimen/float_button_margin"
        android:src="@mipmap/ic_action_new"
        android:onClick="checkPageNum"/>

    </android.support.design.widget.CoordinatorLayout>


    </RelativeLayout>